npm install
npm install mysql2
npm install cookie
npm install multer
npm install jsonwebtoken
npm install lucide-react
npm install -D @types/multer @types/cookie @types/jsonwebtoken

create database= realestate_crm
import database
